"use client";
"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve, reject) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.EngineeringLabWorkspace = EngineeringLabWorkspace;
var link_1 = require("next/link");
var react_1 = require("react");
var navigation_1 = require("next/navigation");
var PrimitiveBadge_1 = require("@/components/design/PrimitiveBadge");
var PrimitiveCard_1 = require("@/components/design/PrimitiveCard");
var CodeEditor_1 = require("@/components/lab/CodeEditor");
var WokwiEmbed_1 = require("@/components/lab/WokwiEmbed");
var WokwiHelpPanel_1 = require("@/components/lab/WokwiHelpPanel");
var SocraticTutorPanel_1 = require("@/components/tutor/SocraticTutorPanel");
var ElectronicsIcons_1 = require("@/components/ui/ElectronicsIcons");
var learningProgressService_1 = require("@/lib/services/learningProgressService");
var exerciseServerActions_1 = require("@/lib/exerciseServerActions");
var practicalValidationService_1 = require("@/lib/services/practicalValidationService");
var LessonReaderStep_1 = require("@/components/lab/LessonReaderStep");
function EngineeringLabWorkspace(_a) {
    var _this = this;
    var _b, _c, _d, _e, _f;
    var initialPracticalState = _a.initialPracticalState, module = _a.module, lessons = _a.lessons, exercises = _a.exercises, _g = _a.socraticQuestions, socraticQuestions = _g === void 0 ? [] : _g, _h = _a.currentUserId, currentUserId = _h === void 0 ? '' : _h, _j = _a.moduleStatus, moduleStatus = _j === void 0 ? 'locked' : _j, _k = _a.nextModuleId, nextModuleId = _k === void 0 ? null : _k;
    var _l = (0, react_1.useState)(''), code = _l[0], setCode = _l[1];
    var _m = (0, react_1.useState)(false), socraticCompleted = _m[0], setSocraticCompleted = _m[1];
    var _o = (0, react_1.useState)(null), progressMessage = _o[0], setProgressMessage = _o[1];
    var _p = (0, react_1.useState)(false), lessonsRead = _p[0], setLessonsRead = _p[1];
    var _q = (0, react_1.useState)((initialPracticalState === null || initialPracticalState === void 0 ? void 0 : initialPracticalState.status) === 'passed' ? 'success' : (initialPracticalState === null || initialPracticalState === void 0 ? void 0 : initialPracticalState.status) === 'failed' ? 'error' : 'idle'), validationState = _q[0], setValidationState = _q[1];
    var _r = (0, react_1.useState)((_b = initialPracticalState === null || initialPracticalState === void 0 ? void 0 : initialPracticalState.score) !== null && _b !== void 0 ? _b : null), validationScore = _r[0], setValidationScore = _r[1];
    var _s = (0, react_1.useState)((initialPracticalState === null || initialPracticalState === void 0 ? void 0 : initialPracticalState.status) === 'passed' ? '✅ Test pratique réussi' : (initialPracticalState === null || initialPracticalState === void 0 ? void 0 : initialPracticalState.status) === 'failed' ? '❌ Projet non validé' : 'Non testé'), validationMessage = _s[0], setValidationMessage = _s[1];
    var _t = (0, react_1.useState)([]), validationIssues = _t[0], setValidationIssues = _t[1];
    var _u = (0, react_1.useState)([]), validationCriteria = _u[0], setValidationCriteria = _u[1];
    var _v = (0, react_1.useState)(0), attemptCount = _v[0], setAttemptCount = _v[1];
    var _w = (0, react_1.useState)(false), showConfirmModal = _w[0], setShowConfirmModal = _w[1];
    var _x = (0, react_1.useState)(false), confirmLoading = _x[0], setConfirmLoading = _x[1];
    var firstExercise = exercises[0];
    var simulationUrl = (_c = firstExercise === null || firstExercise === void 0 ? void 0 : firstExercise.wokwi_project_url) !== null && _c !== void 0 ? _c : null;
    var moduleRequiresPractical = Boolean((firstExercise === null || firstExercise === void 0 ? void 0 : firstExercise.wokwi_project_url) || (firstExercise === null || firstExercise === void 0 ? void 0 : firstExercise.circuit_instructions));
    var router = (0, navigation_1.useRouter)();
    // Keep local flag only for temporary UI acknowledgement (not authoritative)
    var _y = (0, react_1.useState)(false), localModuleCompleted = _y[0], setLocalModuleCompleted = _y[1];
    var isModuleCompleted = moduleStatus === 'completed';
    function handleSocraticComplete() {
        return __awaiter(this, void 0, void 0, function () {
            var error_1;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        if (!currentUserId || !module.id) {
                            setSocraticCompleted(true);
                            setProgressMessage('La phase guidée est marquée comme terminée localement.');
                            return [2 /*return*/];
                        }
                        _a.label = 1;
                    case 1:
                        _a.trys.push([1, 3, , 4]);
                        return [4 /*yield*/, (0, learningProgressService_1.markSocraticCompletionAction)(currentUserId, module.id)];
                    case 2:
                        _a.sent();
                        setSocraticCompleted(true);
                        setProgressMessage('Votre progression a bien été enregistrée pour ce module.');
                        return [3 /*break*/, 4];
                    case 3:
                        error_1 = _a.sent();
                        console.error('[engineering-lab] failed to persist socratic completion', error_1);
                        setProgressMessage('La progression a été marquée localement, mais l’enregistrement a échoué.');
                        setSocraticCompleted(true);
                        return [3 /*break*/, 4];
                    case 4: return [2 /*return*/];
                }
            });
        });
    }
    function handleValidateProject() {
        return __awaiter(this, void 0, void 0, function () {
            var localValidation, result, error_2;
            var _a;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        if (!currentUserId || !(firstExercise === null || firstExercise === void 0 ? void 0 : firstExercise.id)) {
                            setValidationState('error');
                            setValidationScore(null);
                            setProgressMessage('❌ Projet non validé — impossible d’enregistrer la validation sans exercice associé.');
                            return [2 /*return*/];
                        }
                        setAttemptCount(function (value) { return value + 1; });
                        setValidationState('loading');
                        setValidationScore(null);
                        setValidationIssues([]);
                        setValidationCriteria([]);
                        setValidationMessage('Vérification du projet…');
                        setProgressMessage('Validation du projet en cours…');
                        _b.label = 1;
                    case 1:
                        _b.trys.push([1, 3, , 4]);
                        localValidation = (0, practicalValidationService_1.validatePracticalExercise)(code, firstExercise);
                        setValidationCriteria(localValidation.criteriaChecked);
                        setValidationIssues(localValidation.issues);
                        setValidationScore(localValidation.score);
                        return [4 /*yield*/, (0, exerciseServerActions_1.validatePracticalTestAction)(firstExercise.id, code, currentUserId)];
                    case 2:
                        result = _b.sent();
                        if (result.statut === 'approved') {
                            setValidationState('success');
                            setValidationMessage('✅ Test pratique réussi');
                            setValidationScore(result.score);
                            setProgressMessage("✅ Projet validé — la soumission pratique a été enregistrée (".concat(result.score, "/100)"));
                            // Ask Next.js to refresh server-rendered data so `moduleStatus` updates from server-side truth
                            try {
                                router.refresh();
                            }
                            catch (e) {
                                // ignore in non-next runtime
                            }
                        }
                        else {
                            setValidationState('error');
                            setValidationMessage('❌ Projet non validé');
                            setProgressMessage('❌ Projet non validé — corriger les critères indiqués et recommencer le test pratique.');
                            if (((_a = result.issues) === null || _a === void 0 ? void 0 : _a.length) > 0) {
                                setValidationIssues(result.issues.map(function (message) { return ({ severity: 'error', message: message, hint: '' }); }));
                            }
                        }
                        return [3 /*break*/, 4];
                    case 3:
                        error_2 = _b.sent();
                        console.error('[engineering-lab] failed to validate project', error_2);
                        setValidationState('error');
                        setValidationScore(null);
                        setValidationMessage('❌ Projet non validé');
                        setProgressMessage('❌ Projet non validé — la validation a échoué.');
                        setValidationIssues([
                            {
                                severity: 'error',
                                message: 'Erreur serveur lors de la validation du projet.',
                                hint: error_2 instanceof Error ? error_2.message : 'Une erreur inconnue est survenue.',
                            },
                        ]);
                        return [3 /*break*/, 4];
                    case 4: return [2 /*return*/];
                }
            });
        });
    }
    return (<div className="mx-auto flex max-w-7xl flex-col gap-4">/* truncated disabled copy */</div>);
}
